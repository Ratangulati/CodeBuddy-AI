name: "AI Code Review Bot (Gemini)"
description: "Reviews pull requests using Gemini API"
author: "Ratan Gulati"
branding:
  icon: "message-square"
  color: "purple"

inputs:
  gemini_api_key:
    description: "Gemini API key (can be set as secret GEMINI_API_KEY)"
    required: false

runs:
  using: "node20"
  main: "dist/index.js"
